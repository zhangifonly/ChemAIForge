# SQLite 数据库连接字符串 (零配置本地开发)
# 文件相对于 prisma/ 目录
DATABASE_URL="file:./dev.db"

# ---- AI 导师 Claude API（可选）----
# 优先从 CC Switch (~/.cc-switch/cc-switch.db) 读取当前激活的 claude provider。
# 未配置 CC Switch 时，可用以下环境变量兜底（严禁硬编码真实 Key 到代码）：
# CC_SWITCH_DB="/custom/path/cc-switch.db"
# ANTHROPIC_BASE_URL="https://your-claude-endpoint/api"
# ANTHROPIC_AUTH_TOKEN="your-token"
# ANTHROPIC_MODEL="claude-sonnet-4-6"
